<div class="main posts_index">
  <div class="container">
    <% @posts.each do |post| %>
      <div class="posts_index_item">
      <%= link_to(post.content, "/posts/#{post.id}") %>
      </div>
    <% end %>
  </div>
</div>
